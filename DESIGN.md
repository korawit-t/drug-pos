# Drug POS — เอกสารออกแบบ

ระบบขายหน้าร้านยาแบบ open source สำหรับร้านยาแผนปัจจุบันที่มีเภสัชกร ใช้ในร้านเดียว
สถานะ: **ต้นแบบ (prototype)** — ยังไม่ผ่านการตรวจสอบกับร้านจริงหรือกับแบบฟอร์มทางการ

## เป้าหมายของต้นแบบ

ทำหนึ่งเส้นทางให้ครบ เพื่อพิสูจน์ว่าโครงข้อมูลใช้ได้:

รับยาเข้า (lot / วันหมดอายุ) → ขายหน้าร้าน (FEFO, ล็อกยาหมดอายุ, เภสัชกรยืนยันด้วย PIN) → รับเงิน (เงินสด / QR พร้อมเพย์) → พิมพ์ใบเสร็จและฉลากยา → รายงาน ขย.9 และ ขย.11

## สถาปัตยกรรม

```
เครื่อง server ในร้าน (Windows)
  Django + Django Ninja  ← waitress, รันเป็น Windows service
  SQLite (WAL)           ← backend/data/drugpos.sqlite3
        │  LAN  http://<ip-server>:8000
  ┌─────┼─────────┬─────────┐
  เครื่องหน้าร้าน 3–5 เครื่อง: Chrome โหมด kiosk (ไม่ต้องติดตั้งโปรแกรม)
  ├ เครื่องสแกนบาร์โค้ด (ทำงานเหมือนคีย์บอร์ด)
  └ เครื่องพิมพ์ใบเสร็จ 80 มม. / ฉลาก 8×5 ซม. (ผ่าน driver ของ Windows)
```

| ส่วน | ใช้อะไร | เหตุผล |
|---|---|---|
| Backend | Django 6 + Django Ninja | มีหน้า admin ใช้เป็นหลังร้านได้เลย, มีระบบ login/สิทธิ์/migration |
| Database | SQLite | พอสำหรับ 3–5 เครื่อง, สำรองง่าย, ย้ายไป PostgreSQL ผ่าน ORM ได้ |
| Frontend | React + Vite | หน้าขายต้องเร็วและใช้คีย์บอร์ดเป็นหลัก |
| Web server | waitress | gunicorn รันบน Windows ไม่ได้ |
| Login | Django session + CSRF | หน้าเว็บกับ API อยู่ origin เดียวกัน ไม่ต้องใช้ JWT |

ทุกอย่างทำงานในร้านได้โดยไม่ต้องต่ออินเทอร์เน็ต (ไม่มี CDN หรือ font จากภายนอก)

## โครงข้อมูล

| ตาราง | เก็บอะไร |
|---|---|
| `inventory.Product` | ข้อมูลยา: ชื่อการค้า, ชื่อสามัญ, ความแรง, ประเภทยา, flag ขย.11/ขย.13, หน่วยเล็กสุด, วิธีใช้/คำเตือนบนฉลาก |
| `inventory.ProductUnit` | หน่วยขาย: ตัวคูณเทียบหน่วยเล็กสุด, บาร์โค้ด, ราคา 5 ระดับ |
| `inventory.PriceLevel` | ชื่อระดับราคา 1–5 (มีครบ 5 แถวเสมอ เปลี่ยนได้แค่ชื่อ) |
| `inventory.Lot` | lot + วันหมดอายุ + ยอดคงเหลือ (cache ของ ledger) |
| `inventory.StockMovement` | สมุดบัญชีสต็อก: ทุกการเข้า-ออก 1 แถว ไม่แก้ ไม่ลบ |
| `inventory.Purchase` / `PurchaseItem` | ใบรับยา / ยอดยกมา → ขย.9 (ร่างเก็บ lot, วันหมดอายุ, ราคาทุนที่ยังว่างได้) |
| `sales.Customer` / `CustomerAllergy` | ลูกค้า: ระดับราคา, โรคประจำตัว / ประวัติแพ้ยาทีละรายการ (ยา/กลุ่มยา, อาการ, ความรุนแรง) |
| `inventory.Allergen` | กลุ่มยาสำหรับจับคู่การแพ้ (คำที่ใช้จับคู่ + กลุ่มที่อาจแพ้ข้ามกัน) |
| `sales.Sale` / `SaleItem` | บิลและรายการขาย (เก็บ snapshot ชื่อยา ราคา ประเภท ณ ตอนขาย) |
| `sales.SaleItemLot` | แต่ละรายการตัดจาก lot ไหนเท่าไร (1 รายการอาจตัดหลาย lot) |
| `sales.HeldBill` | บิลที่พักไว้ |
| `accounts.User` | ผู้ใช้ + บทบาท (พนักงาน/เภสัชกร) + PIN |
| `accounts.PinAttempt` | ประวัติการใส่ PIN ทุกครั้ง ทั้งผ่านและไม่ผ่าน |
| `core.ShopSettings` | ข้อมูลร้าน, เบอร์พร้อมเพย์, จำนวนวันเตือนยาใกล้หมดอายุ |

## กติกาที่ต้องรักษาไว้

- **จำนวนเก็บเป็นจำนวนเต็มในหน่วยเล็กสุด** (เม็ด/แคปซูล/ขวด) แปลงเป็นแผง/กล่องตอนแสดงผลเท่านั้น
- **เงินใช้ Decimal** ฝั่ง server และใช้สตางค์ (จำนวนเต็ม) ฝั่ง React — ไม่ใช้ float
- **สต็อกเปลี่ยนได้ทางเดียว** คือ `inventory.services.record_movement()` ซึ่งเขียน ledger และปรับยอดคงเหลือใน transaction เดียวกัน — `python manage.py check_stock` ตรวจว่ายอดตรงกับ ledger
- **FEFO**: ตัดจาก lot ที่หมดอายุก่อน; lot ขายได้ถึงวันหมดอายุ (วันหมดอายุ < วันนี้ = ขายไม่ได้)
- **รับยาที่หมดอายุแล้วเข้าไม่ได้** ยกเว้นใบ "ยอดยกมา" (นับของบนชั้นตอนเริ่มใช้ระบบ)
- **ใบรับยาที่บันทึกเข้าสต็อกแล้วแก้ไม่ได้** และบิลขายไม่ลบ — ยกเลิกบิลคือเปลี่ยนสถานะ + ลง movement คืนเข้า lot เดิม
- **ราคาคำนวณใหม่ที่ server ทุกครั้ง** ตัวเลขฝั่งหน้าจอเป็นแค่ตัวอย่าง
- **วันที่เก็บเป็น ค.ศ.** แสดงเป็น พ.ศ. บนใบเสร็จ ฉลาก และรายงาน
- **SQLite ตั้งเป็น WAL + `BEGIN IMMEDIATE`** ทุก transaction จองสิทธิ์เขียนก่อน จึงไม่มีทางที่สองเครื่องขายยากล่องสุดท้ายพร้อมกันได้
- **checkout ส่งซ้ำได้อย่างปลอดภัย** — บิลผูกกับ `client_uuid` ที่หน้าจอสร้างตอนเริ่มบิล

## นำเข้าข้อมูลจาก Excel

`inventory/importer.py` + หน้า `/#/import` (เฉพาะผู้ดูแลร้าน) สำหรับตอนเริ่มใช้ระบบและตอนปรับราคาทั้งร้าน

- **1 แถว = 1 หน่วยขาย** ยาตัวเดียวกันรวมจากชื่อการค้า + ความแรง รับทั้ง `.xlsx` และ CSV (utf-8 หรือ cp874 ที่ Excel ไทยบันทึกมา)
- **ตรวจก่อนเขียนเสมอ**: dry run เดินโค้ดเส้นเดียวกับการนำเข้าจริงแล้ว rollback ตัวอย่างที่เห็นจึงตรงกับของจริง และถ้ามีแถวผิดแม้แถวเดียวจะไม่เขียนอะไรเลย
- **นำเข้าซ้ำได้**: จับคู่ด้วยบาร์โค้ดก่อน ถ้าไม่มีใช้ชื่อการค้า + ความแรง; ช่องที่เว้นว่างไม่ทับข้อมูลเดิม; บาร์โค้ดที่เป็นของยาตัวอื่นอยู่แล้วถือเป็นข้อผิดพลาด
- **ยอดยกมา** (เลือกได้): สร้างใบรับยาแบบยอดยกมาหนึ่งใบแล้ว post เข้าสต็อกผ่านทางเดิม — ทุกอย่างจึงอยู่ใน ledger; นำเข้าไฟล์เดิมซ้ำจะบวกสต็อกซ้ำ หน้าจอจึงเตือนไว้
- ไฟล์ตัวอย่างสร้างจากโค้ด (`template_workbook`) จึงตรงกับคอลัมน์ที่โปรแกรมอ่านเสมอ และมีเทสต์ว่านำเข้ากลับได้

## การรับยาเข้า

หน้าจอ React (`frontend/src/receive/`) ใช้ API `/api/purchases` และ service `inventory.services.save_purchase`

- **ร่าง**: บันทึกได้แม้ยังกรอกไม่ครบ ยังไม่แตะสต็อก แก้ไขหรือลบได้
- **บันทึกเข้าสต็อก**: ตรวจครบทุกช่องแล้วบันทึกและ post ใน transaction เดียว ถ้าพลาดรายการใดจะไม่บันทึกอะไรเลย บันทึกแล้วแก้หรือลบไม่ได้
- **ราคาทุนว่าง ≠ 0**: ว่าง = ยังไม่กรอก (ร่างเท่านั้น) ส่วน 0 = ของแถม; ยอดยกมาเว้นว่างได้ (คิดเป็น 0)
- **วันหมดอายุพิมพ์ตามกล่อง**: `03/2028` = สิ้นเดือนนั้น, ปี 2 หลัก 60–99 = พ.ศ. 00–59 = ค.ศ., ปี 4 หลักเกิน 2400 = พ.ศ. — หน้าจอแสดงวันที่ที่อ่านได้ให้ตรวจทุกครั้ง
- **กันพลาด**: ใบกำกับเลขเดิมของผู้ขายรายเดิมบันทึกซ้ำไม่ได้, วันที่รับยาเป็นวันในอนาคตไม่ได้, เตือนเมื่อยาใกล้หมดอายุ และเมื่อเลข lot ตรงกับที่มีในสต็อกแต่วันหมดอายุไม่ตรง
- **ราคาทุนครั้งล่าสุด** ของหน่วยนั้น (จากใบรับยาที่บันทึกแล้ว ไม่นับยอดยกมา) เติมให้อัตโนมัติ
- หน้าขายและหน้ารับยาเปิดค้างไว้ทั้งคู่ สลับแท็บแล้วบิลที่กำลังทำไม่หาย ปุ่ม F ของหน้าขายไม่ทำงานตอนอยู่หน้ารับยา

## แจ้งเตือนแพ้ยา

โค้ดจับคู่อยู่ที่ `inventory/allergy.py` ตรวจทั้งตอนแสดงผล (`/api/customers/{id}/allergy-check`) และตอน checkout

- ทั้งประวัติแพ้ยาและตัวยาถูกแปลงเป็น **กลุ่มยา** จากชื่อ (ชื่อสามัญ/ชื่อการค้า) หรือจากกลุ่มที่เลือกไว้ตรง ๆ เช่น บันทึกว่า "แพ้ Penicillin" → กลุ่ม Penicillins → เตือน Amoxicillin
- **แพ้โดยตรง** (สีแดง): ยาอยู่ในกลุ่มเดียวกับที่แพ้ หรือชื่อยาตรงกับข้อความที่บันทึก · **อาจแพ้ข้ามกลุ่ม** (สีเหลือง): กลุ่มที่ตั้งไว้ว่าอาจแพ้ข้ามกัน เช่น Penicillins–Cephalosporins, Aspirin–NSAIDs
- คำภาษาอังกฤษจับคู่ทั้งคำ ("sulfa" ไม่ไปโดน "sulfate") ภาษาไทยจับคู่ในข้อความได้เลยเพราะไม่มีช่องว่าง
- ยาที่เตือน **ต้องให้เภสัชกรใส่ PIN และเขียนเหตุผล** แม้เป็นยาสามัญประจำบ้าน หรือกดเอาออกจากบิล — token ของเภสัชกรผูกกับลูกค้าคนนั้นและข้อความเหตุผล; เหตุผลและข้อความเตือนเก็บไว้กับรายการขาย
- ยืนยันไว้ก่อนแล้วค่อยเลือกลูกค้าที่แพ้ → การยืนยันเดิมถูกยกเลิก ต้องยืนยันใหม่
- บันทึกแพ้ยาได้จากหน้าขาย (การ์ดลูกค้า → "+ บันทึกแพ้ยา") หรือหลังร้าน; รายการที่จับคู่กลุ่มไม่ได้จะขึ้นป้ายให้รู้ว่าเตือนเฉพาะชื่อตรง
- **กลุ่มยาตั้งต้น 13 กลุ่มเป็นค่าเริ่มต้นเท่านั้น เภสัชกรต้องตรวจทาน** แก้คำจับคู่/เพิ่มกลุ่มได้ที่หลังร้าน และที่หน้ายาแต่ละตัวจะแสดงว่าระบบจับคู่กลุ่มอะไรได้ (ยาชื่อการค้าที่ไม่บอกชื่อสามัญให้เลือกกลุ่มเอง)
- ลูกค้าทั่วไป (ไม่ได้เลือกลูกค้า) ไม่มีข้อมูลให้ตรวจ — หน้าต่างเภสัชกรเตือนให้ถามลูกค้าเอง

## ระดับราคา 5 ระดับ

- แต่ละหน่วยขายมีราคา 5 ช่อง; ระดับ 1 (ราคาปลีก) บังคับ, ระดับ 2–5 เว้นว่างได้ → ถ้าว่างใช้ราคาระดับ 1
- ลูกค้าแต่ละคนมีระดับราคาประจำตัว เลือกลูกค้าแล้วบิลเปลี่ยนเป็นระดับนั้นอัตโนมัติ และเปลี่ยนระดับในบิลได้
- ชื่อระดับตั้งได้ที่หลังร้าน (ตัวอย่าง: ราคาปลีก, นักศึกษา/บุคลากร, สมาชิก, คลินิก/ร้านยา, ราคาพิเศษ)

## การยืนยันของเภสัชกร

ร้านจริงมัก login ค้างไว้ user เดียว จึงผูกการยืนยันกับ **PIN ส่วนตัวของเภสัชกร** ไม่ใช่กับคนที่ login

| ประเภทยา | ต้องใส่ PIN | ข้อมูลเพิ่ม |
|---|---|---|
| สินค้าทั่วไป, ยาสามัญประจำบ้าน, ยาบรรจุเสร็จ | ไม่ต้อง | – |
| ยาอันตราย | ต้อง | – |
| ยาอันตรายที่ติ๊ก "ต้องลง ขย.11" | ต้อง | ชื่อผู้ซื้อ |
| ยาควบคุมพิเศษ | ต้อง | ข้อมูลใบสั่งยา (ผู้สั่งจ่าย, สถานพยาบาล) |

ขั้นตอน: พนักงานสแกนยา → ยาที่ต้องยืนยันขึ้น "รอเภสัชกรยืนยัน" → กด F12 → หน้าต่างเภสัชกรแสดงเฉพาะรายการที่ต้องยืนยัน พร้อม lot และวิธีใช้ที่จะพิมพ์ลงฉลาก (แก้ได้) → เภสัชกรเลือกชื่อตัวเอง ใส่ PIN → ไปหน้ารับเงิน (กด F8 เพื่อยืนยันก่อนก็ได้)

กลไก: เมื่อ PIN ถูก server ออก token ที่ลงลายเซ็น (Django signing) ให้แต่ละรายการ ผูกกับ บิล + หน่วย + จำนวน + ข้อความฉลาก และหมดอายุใน 30 นาที ตอน checkout server ตรวจ token ทุกรายการอีกครั้ง

- แก้จำนวน หน่วย หรือข้อความฉลากหลังยืนยัน → ต้องยืนยันใหม่ (ทั้งฝั่งหน้าจอและฝั่ง server)
- เภสัชกรไม่อยู่ → ไม่มีปุ่มข้าม ทำได้แค่พักบิลหรือเอารายการออก
- ไม่มีการยืนยันจากเครื่องอื่น เภสัชกรต้องใส่ PIN ที่เครื่องที่ขาย
- PIN 4–6 หลัก แยกจากรหัสผ่าน; ผิด 5 ครั้งล็อก 5 นาที; บันทึกทุกครั้งที่ใส่
- ยกเลิกบิลใช้ PIN เภสัชกร + เหตุผล (กลไกเดียวกัน)

## การรับเงิน

- **เงินสด**: ปุ่มจำนวนเงินด่วน, คำนวณเงินทอน
- **โอน / QR พร้อมเพย์**: สร้าง QR ระบุยอด (มาตรฐาน EMVCo / Thai QR) ในเครื่องเอง ไม่ต่อธนาคาร — พนักงานต้องตรวจว่าเงินเข้าก่อนกดยืนยัน; ตั้งเบอร์หรือเลขผู้เสียภาษีที่หลังร้าน → ข้อมูลร้าน

## รายงาน

| รายงาน | สถานะ |
|---|---|
| ขย.9 บัญชีการซื้อยา | ทำแล้ว (จากใบรับยา ไม่รวมยอดยกมา) |
| ขย.11 บัญชีการขายยาอันตรายตามที่ อย. ประกาศ | ทำแล้ว (เฉพาะยาที่ติ๊ก ขย.11, ไม่รวมบิลยกเลิก) |
| ยาหมดอายุ / ใกล้หมดอายุ | ทำแล้ว |
| ขย.10, ขย.12, ขย.13 | ยังไม่ทำ — ข้อมูลที่ต้องใช้เก็บไว้ครบแล้วในทุกบิล |

**ต้องเทียบคอลัมน์กับแบบฟอร์มทางการของ อย. ก่อนใช้งานจริง** และรายการยาที่ต้องลง ขย.11/ขย.13 ร้านต้องตั้งเองตามประกาศฉบับล่าสุด

## ยังไม่ได้ทำ (ขั้นต่อไป)

- ยาตีกัน (drug interaction) และแจ้งเตือนยาซ้ำซ้อนในบิล
- ปรับยอดสต็อก (นับสต็อก), คืนสินค้าให้ผู้ขาย, เพิ่มสินค้าใหม่จากหน้ารับยา (ตอนนี้ต้องไปหลังร้าน)
- ส่วนลด (ต้องใช้ PIN เมื่อเกินกำหนด), ใบกำกับภาษี/VAT
- ขย.10, ขย.12, ขย.13
- ชุดข้อมูลยาตั้งต้น (TMT) — ต้องตรวจเงื่อนไขการเผยแพร่ก่อน
- เครื่องอ่านบัตรประชาชน, เชื่อม สปสช.
- ความยินยอมตาม PDPA สำหรับข้อมูลสุขภาพลูกค้า

## คำถามที่ยังเปิดอยู่

- ช่องข้อมูลที่แบบฟอร์ม ขย. ต้องการจริง (ต้องตรวจกับแบบฟอร์มฉบับปัจจุบัน)

License: MIT (ตัดสินใจเมื่อ 21 ก.ย. 2569)
